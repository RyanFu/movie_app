- records.each do |r|
  .review
    = link_to image_tag(user_avatar(r.user.fb_id)), user_path(r.user)
    = r.comment
    like count
    = r.love_count
    comments_count
    = r.comments_count
    create
    = r.created_at